# macOS
.DS_Store

# Logs / temp
*.log
node_modules/

# Local backups exported from the app (never commit personal data)
*expense-tracker-backup-*.json
transactions-*.csv

# Bank sync: credentials, tokens, link state, and fetched data stay local
sync/.env
sync/.tokens.json
sync/state.json
bank-sync-*.json

# Python bytecode
__pycache__/
*.pyc

# Pasted bank statements used only for local testing
test.txt
*.paste.txt
test.json
